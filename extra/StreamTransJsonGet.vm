{
  Object arbitobj =  $1.getBody();
  String keyStr = null;
  if(arbitobj instanceof String )
   keyStr = (String)arbitobj;
   else if(arbitobj instanceof byte[] )
      keyStr   = new String((byte[])arbitobj);
   else
   	   keyStr = arbitobj.toString();   
net.sf.json.JSONObject jsonObject = net.sf.json.JSONObject.fromObject(keyStr);
$objtype e = new $objtype();
$codestr
return e;
}